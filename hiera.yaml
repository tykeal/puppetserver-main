---
:backends:
  - eyaml

:hierarchy:
  - "%{::domain}/%{::hostname}"
  - "%{::domain}/common"
  - common

:eyaml:
  :datadir: "/etc/puppetlabs/code/hieradata/%{::environment}"
  :gpg_gnupghome: '/etc/puppetlabs/gnupg'
